fastapi>=0.111
uvicorn[standard]>=0.29
pydantic>=2
python-multipart>=0.0.9
pypdf>=4.2
ollama>=0.3
numpy>=1.26
faiss-cpu>=1.8.0.post1

# OCR (optional but recommended for scanned PDFs)
pytesseract>=0.3.10
pdf2image>=1.17.0
pillow>=10

# pgvector + Postgres (optional, for large-scale vector DB)
psycopg[binary,pool]>=3.2
pgvector>=0.2.5

# LLM via Groq (preferred when GROQ_API_KEY is provided)
groq>=0.9.0

# Local embeddings (high quality, no API key)
sentence-transformers>=3.0.1

# Load .env for local development
python-dotenv>=1.0.1
